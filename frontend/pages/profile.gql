query GetProfile($id: Int!) {
  getProfile(id: $id) {
    id
    name
    email
    role
    tests {
      id
      name
    }
    results {
      id
      score
      test {
        id
        name
      }
    }
  }
}

mutation EditProfile($input: EditProfileInput!) {
  editProfile(input: $input) {
    id
    name
    email
    role
    tests {
      id
      name
    }
    results {
      id
      score
    }
  }
}
